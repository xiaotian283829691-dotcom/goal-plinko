/**
 * Game configuration for the free-to-play credit system.
 */

/** Credit amounts available for betting. */
export const CREDIT_BET_OPTIONS = [1, 2, 5, 10] as const;

/** Free credits given to every player on start (and on refill). */
export const FREE_CREDITS = 30;
