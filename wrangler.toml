name = "goal-plinko"
compatibility_date = "2025-05-01"

[site]
bucket = "./dist"

# Pages-specific build settings (used by `wrangler pages deploy`)
# Build command and output are also configured in the Cloudflare Pages dashboard,
# but keeping them here for documentation.
# build_command = "npm run build"
# build_output_dir = "dist"
